[build]
publish = "public"
command = "make production-build"

[build.environment]
HUGO_VERSION = "0.75.1"

[context.deploy-preview]
command = "echo 'User-agent: *\nDisallow: /' > layouts/robots.txt && make preview-build"

[context.branch-deploy]
command = "echo 'User-agent: *\nDisallow: /' > layouts/robots.txt && make preview-build"
